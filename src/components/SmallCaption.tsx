const SmallCaption = (): React.JSX.Element => (
  <figcaption className="blockquote-footer">
    <cite>Please verify that data to send are correct.</cite>
  </figcaption>
);

export default SmallCaption;
